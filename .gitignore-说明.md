# .gitignore 文件说明

## 📋 概述

本项目的`.gitignore`文件已配置完成，用于排除不需要版本控制的文件和目录。

---

## ✅ 验证结果

已验证以下文件/目录被正确忽略：
- ✅ `logs/` - 所有日志文件
- ✅ `target/` - Maven编译输出
- ✅ `.DS_Store` - macOS系统文件

---

## 📂 主要忽略的内容

### 1. 构建输出
```
target/              # Maven构建目录
*.class              # Java编译文件
*.jar                # JAR包（除了Maven wrapper）
*.war, *.ear         # 部署包
```

### 2. IDE配置文件
```
.idea/               # IntelliJ IDEA
*.iml                # IDEA模块文件
.vscode/             # VS Code
.settings/           # Eclipse
.classpath           # Eclipse
.project             # Eclipse
```

### 3. 日志和数据文件
```
logs/                # 所有日志目录
*.log                # 日志文件
*.h2.db              # H2数据库文件
data/*.db            # 数据文件
```

### 4. 输出文件
```
output/              # 应用输出目录
index-output.json    # 索引输出文件
```

### 5. 系统文件
```
.DS_Store            # macOS
Thumbs.db            # Windows
*~                   # Linux备份文件
```

### 6. 安全敏感文件
```
.env                 # 环境变量
*.key, *.pem         # 密钥文件
*.jks, *.p12         # 密钥库
application-secret.* # 敏感配置
secrets/             # 密钥目录
```

### 7. 临时文件
```
*.tmp, *.temp        # 临时文件
*.bak, *.backup      # 备份文件
*.swp                # Vim交换文件
*.pid                # 进程ID文件
```

---

## 🔍 验证忽略规则

### 检查特定文件是否被忽略
```bash
git check-ignore -v <文件路径>
```

示例：
```bash
# 检查日志文件
git check-ignore -v logs/application.log

# 检查target目录
git check-ignore -v target/

# 检查多个文件
git check-ignore -v logs/*.log target/ .DS_Store
```

### 查看所有未跟踪的文件
```bash
git status
```

### 查看被忽略的文件列表
```bash
git status --ignored
```

---

## 📝 常用Git命令

### 初始化仓库（如果还未初始化）
```bash
git init
```

### 添加所有文件到暂存区
```bash
git add .
```

### 查看将要提交的文件
```bash
git status
```

### 提交更改
```bash
git commit -m "Initial commit: Spring Boot batch processing project"
```

### 添加远程仓库
```bash
git remote add origin <远程仓库URL>
```

### 推送到远程仓库
```bash
git push -u origin master
# 或者使用main分支
git branch -M main
git push -u origin main
```

---

## 🛠️ 自定义配置

### 需要忽略额外文件
编辑`.gitignore`文件，添加新的规则：
```bash
# 忽略特定文件
my-config.local

# 忽略特定目录
custom-output/

# 忽略特定扩展名
*.local
```

### 需要强制添加被忽略的文件
```bash
git add -f <文件路径>
```

### 清除已跟踪但现在被忽略的文件
```bash
# 从Git中删除，但保留本地文件
git rm -r --cached target/
git rm --cached logs/*.log

# 提交更改
git commit -m "Remove ignored files from tracking"
```

---

## ⚠️ 特别注意

### 已提交的文件
`.gitignore`只对**未跟踪的文件**生效。如果文件已经被Git跟踪，需要先从仓库中删除：

```bash
# 1. 从Git中删除但保留本地文件
git rm --cached <文件路径>

# 2. 提交删除操作
git commit -m "Remove tracked file"

# 3. 现在.gitignore规则会生效
```

### 保留的文件
以下文件**不会被忽略**（应该提交到仓库）：
- ✅ 源代码 (`src/`)
- ✅ 配置文件 (`application.yml`, `pom.xml`)
- ✅ SQL脚本 (`sql/`)
- ✅ 文档 (`docs/`, `README*.md`)
- ✅ Docker配置 (`Dockerfile`, `docker-compose.yml`)
- ✅ K8s配置 (`k8s/`)
- ✅ 脚本 (`scripts/`)
- ✅ Maven wrapper（如果需要）

---

## 📊 当前仓库状态

查看当前Git状态：
```bash
cd /Users/cailiang/Desktop/java/index
git status
```

主要未跟踪的文件（应该提交）：
```
- .gitignore              # Git忽略规则
- Dockerfile*             # Docker配置
- README*.md              # 项目文档
- pom.xml                 # Maven配置
- src/                    # 源代码
- scripts/                # 脚本文件
- sql/                    # SQL脚本
- k8s/                    # K8s配置
- docs/                   # 文档
- docker-compose.yml      # Docker Compose
- settings.xml            # Maven设置
- env.example             # 环境变量示例
```

自动忽略的文件（不会提交）：
```
- target/                 # 构建输出
- logs/                   # 日志文件
- .DS_Store               # 系统文件
- .idea/                  # IDE配置（如果存在）
- *.class                 # 编译文件
```

---

## 🚀 推荐的提交流程

### 首次提交
```bash
# 1. 初始化仓库（如果还未初始化）
git init

# 2. 添加所有文件
git add .

# 3. 查看将要提交的文件
git status

# 4. 创建首次提交
git commit -m "Initial commit: Spring Boot batch processing project

Features:
- Spring Batch integration
- H2 database support
- Elasticsearch indexing
- Quartz scheduler
- Docker and K8s configuration
- Monitoring and metrics"

# 5. 添加远程仓库
git remote add origin <你的仓库URL>

# 6. 推送到远程
git push -u origin master
```

### 日常提交
```bash
# 1. 查看更改
git status

# 2. 添加更改的文件
git add <文件路径>
# 或添加所有更改
git add .

# 3. 提交
git commit -m "描述你的更改"

# 4. 推送
git push
```

---

## 🔗 相关资源

- [Git官方文档](https://git-scm.com/doc)
- [.gitignore模板](https://github.com/github/gitignore)
- [Git忽略规则语法](https://git-scm.com/docs/gitignore)

---

## 📞 常见问题

### Q: 为什么某些文件仍然显示在git status中？
A: 这些文件可能已经被Git跟踪了。使用`git rm --cached <文件>`从跟踪中删除。

### Q: 如何忽略整个目录？
A: 在`.gitignore`中添加`目录名/`（注意末尾的斜杠）。

### Q: 如何忽略除某个文件外的所有文件？
A: 使用否定模式：
```
# 忽略所有.log文件
*.log
# 但保留important.log
!important.log
```

### Q: 忽略规则不生效？
A: 确保：
1. 文件还未被Git跟踪
2. `.gitignore`文件在仓库根目录
3. 规则语法正确
4. 清除Git缓存：`git rm -r --cached . && git add .`

---

**创建时间**: 2025-10-14  
**适用项目**: Spring Boot Batch Processing Index Service


# 🚀 测试组合 - 快速参考

## 一键运行

### 方式1️⃣: IDEA
```
右键 AllCombinationsTestSuite → Run
```

### 方式2️⃣: Maven
```bash
mvn test -Dtest=AllCombinationsTestSuite
```

### 方式3️⃣: 脚本
```bash
./scripts/run-all-combination-tests.sh
```

---

## 🧪 测试类速查

| 测试类 | 数据源 | 索引目标 | 运行命令 |
|--------|--------|---------|---------|
| `FullIndexJobTest` | H2 | File | `mvn test -Dtest=FullIndexJobTest` |
| `IncrementalIndexJobTest` | H2 | File | `mvn test -Dtest=IncrementalIndexJobTest` |
| `CsvToFileJobTest` | CSV | File | `mvn test -Dtest=CsvToFileJobTest` |
| `JsonToFileJobTest` | JSON | File | `mvn test -Dtest=JsonToFileJobTest` |
| `H2ToElasticsearchJobTest` | H2 | ES | (默认禁用) |

---

## 📁 文件位置

```
src/test/
├── java/.../batch/job/
│   ├── base/AbstractJobTest.java         # 基类
│   ├── FullIndexJobTest.java             # H2→File 全量
│   ├── IncrementalIndexJobTest.java      # H2→File 增量
│   ├── CsvToFileJobTest.java             # CSV→File
│   ├── JsonToFileJobTest.java            # JSON→File
│   ├── H2ToElasticsearchJobTest.java     # H2→ES (Mock)
│   └── AllCombinationsTestSuite.java     # 测试套件
└── resources/
    ├── test-data/
    │   ├── sample-data.csv               # CSV数据
    │   └── sample-data.json              # JSON数据
    └── application-test-*.yml            # 测试配置
```

---

## 📊 测试矩阵

```
        │  File  │   ES    │ GetQuick
────────┼────────┼─────────┼──────────
H2      │   ✅   │  ⚠️(M)  │   🚫
MySQL   │   🔧   │   🔧    │   🚫
Oracle  │   🔧   │   🔧    │   🚫
CSV     │   ✅   │   ⚠️    │   🚫
JSON    │   ✅   │   ⚠️    │   🚫

图例:
✅ = 已实现  ⚠️ = 需要外部服务
🔧 = 待实现  🚫 = 跳过  (M) = Mock
```

---

## 🔍 常见问题

### Q: 如何只运行CSV测试？
```bash
mvn test -Dtest=CsvToFileJobTest
# 或
./scripts/run-all-combination-tests.sh csv
```

### Q: 如何启用ES测试？
1. 启动Elasticsearch服务
2. 移除 `@Disabled` 注解
3. 配置ES连接参数

### Q: 测试数据在哪？
- H2: 自动生成（`@BeforeEach`）
- CSV: `test-data/sample-data.csv`
- JSON: `test-data/sample-data.json`

### Q: 如何添加新组合？
1. 创建配置文件 `application-test-<src>-<tgt>.yml`
2. 创建测试类继承 `AbstractJobTest`
3. 添加到 `AllCombinationsTestSuite`

详见: [测试组合说明.md](docs/测试组合说明.md)

---

## 📖 完整文档

- [测试组合说明](docs/测试组合说明.md) - 完整指南
- [测试组合框架完成](测试组合框架完成.md) - 实现总结

---

**🎯 现在就试试吧！** 
右键 `AllCombinationsTestSuite` → Run

